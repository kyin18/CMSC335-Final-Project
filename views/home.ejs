<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css">
    <style>
        .hero {
            text-align: center;
            padding: 60px 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 10px;
            margin: 30px 0;
        }
        .hero h1 {
            font-size: 3rem;
            margin-bottom: 20px;
        }
        .hero p {
            font-size: 1.2rem;
            margin-bottom: 40px;
            opacity: 0.9;
        }
        .options {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 40px;
        }
        .option-card {
            background: white;
            padding: 30px;
            border-radius: 10px;
            width: 300px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transition: transform 0.3s;
        }
        .option-card:hover {
            transform: translateY(-5px);
        }
        .option-card i {
            font-size: 3rem;
            color: #667eea;
            margin-bottom: 20px;
        }
        .option-card h3 {
            margin-bottom: 15px;
            color: #333;
        }
        .option-card p {
            color: #666;
        }
        .btn {
            display: inline-block;
            padding: 12px 30px;
            background: #667eea;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }
        .btn:hover {
            background: #5a6fd8;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="hero">
            <h1>Task Manager + Weather Checker</h1>
            <p>Manage your tasks and check weather forecasts for each task</p>
        </div>
        
        <div class="options">
            <div class="option-card">
                <h3>Add New Task</h3>
                <a href="/add" class="btn">Go to Add Task</a>
            </div>
            
            <div class="option-card">
                <h3>View All Tasks</h3>
                <a href="/tasks" class="btn">Go to Tasks</a>
            </div>
        </div>
    </div>
</body>
</html>